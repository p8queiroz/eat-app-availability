
export const getRestaurantData = (state: any) => state.restaurantData.restaurant;

//export const getUserById = (id: number) => createSelector([getUsersData], (users: User[]) => users && users.find(((user: User) => user.id === id)));
